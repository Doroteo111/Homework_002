using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EXERCISE_7 : MonoBehaviour
{
    // Create a string variable that is supposed to store only the values “sum”, “subtraction”, “product”
    // and “division”. Create two float variables.If the value of the string variable is “sum”, then
    // display in the console the sum of the two float variables. If the value of the string variable is
    // “subraction”, then display in the console the subtraction of the two float variables. And the
    // same for the “product” and “division” values

    //For example, if we specify that the value of the string variable is “product” and the two  float
    //variables are 7 and 10, then the following should be displayed: “7 x 10 = 70”
}
